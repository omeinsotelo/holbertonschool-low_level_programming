#include "holberton.h"
/**
*_isalpha - checks for alphabetic character
*@c: The alphabetics character to check
*Return: 1 if is a leter lowercase or uppercase and 0 otherwise
*/
int _isalpha(int c)
{
  if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
{
return (1);
}
else
{
return (0);
}
}
