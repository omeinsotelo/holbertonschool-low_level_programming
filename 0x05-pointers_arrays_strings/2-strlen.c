#include "holberton.h"
/**
 *_strlen - function return the lengh of a string
 *@s: the string
 *Return: length of a string
 */
int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != '\0'; c++)
	{

	}
	return (c);
}
