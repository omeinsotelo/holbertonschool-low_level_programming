#include "holberton.h"
#include <stdio.h>
/**
 *print_array - function that prints other characters
 *@dest: the parameter
 *@src: the parameter
 *Return: none
 */
char *_strcpy(char *dest, char *src)
{
	int a, t, b;

	b = 0;

	for (a = 0; s[a] != 0; a++)
	{
	}
	a--;
	while (b < a)
	{
		t = s[b];
		s[b] = s[a];
		s[a] = t;
		b++;
		a--;
	}
}
